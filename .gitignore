__pycache__/
*.pyc
.DS_Store
# personal local Claude Code settings (permissions, etc.)
.claude/settings.local.json
# run outputs (transcripts, digests, reports, PDFs)
/out/
*.pdf
# ...but keep committed sample artifacts
!examples/*.pdf
